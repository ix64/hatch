name: {{ .BinaryName }}_dev

services:
  postgres:
    image: "postgres:18"
    restart: unless-stopped
    ports:
      - "0.0.0.0:25432:5432"
    environment:
      POSTGRES_DB: "app"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
