# {{ .AppName }}

由 `hatch` 初始化的 Go 服务骨架。

## 快速开始

```bash
hatch tools install
hatch gen ent
hatch env start
go run ./cmd/server serve
```

常用命令已经收敛到 `hatch` CLI：

- `hatch build`
- `hatch start`
- `hatch dev`
- `hatch env add minio`
- `hatch migrate generate --name init`
- `hatch gen rpc`
